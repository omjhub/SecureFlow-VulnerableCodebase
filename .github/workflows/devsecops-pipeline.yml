name: DevSecOps Pipeline

on:
  push:
  pull_request:

jobs:
  hello-world:
    name: Hello World (pipeline skeleton verification)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Say hello
        run: echo "SecureFlow DevSecOps pipeline skeleton is running."

      - name: Confirm repository checkout worked
        run: ls -la
